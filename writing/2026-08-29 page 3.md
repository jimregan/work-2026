
## Sustainability

~~Although sustainability is not  
the prime~~

Sustainability is one of the driving motivators of this work: to be able to transition ~~it~~ from a snapshot-oriented approach, with ad hoc processing, to an incremental approach with repeatable transformations that are not merely repeatable, but (repeatable by design).

The transforms are intended to be driven primarily through configuration files, so that the variables and implementation are held separate, without restricting understanding to programmers. The metadata that drives the graph is…