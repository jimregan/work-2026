Some LaTeX matters:

In section 3.3 you have “p-value ¡ 0.05”: use dollar signs to escape the less-than sign: “p-value $<$ 0.05” (or “p-value $< 0.05$”)

Opening quotation marks are written using backticks: ``this is properly quoted''

In the legend of table 1: “mean weighted F1-Scorewith” is missing a space betwen “Score” and “with”.

Section 4.5:

> Attempts to reduce noise using Praats spectral

Praat’s

Section 5: 

> The studys results

study’s

> evauate broder techniques

evaluate broader techniques

— 


This paper investigates the impact of various preprocessing techniques on the performance of a deep learning model for dialect classification, focusing on RMS amplitude, DC-offset, articulation rate (AR), pitch, and Harmonics-to-Noise Ratio (HNR). The authors explore whether preprocessing contributes to performance improvement or if deep learning models inherently handle variations in raw audio inputs.

The neural network architecture employed is somewhat dated, but this is not a drawback