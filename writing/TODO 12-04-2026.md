
- [ ] Swedish dialect classifier
	- Mismatch between common voice and NST
	- NST has region of birth, current region
	- Common Voice started with a list, but has moved to a freeform text input
- [ ] English age classifier
	- [ ] Common Voice has decades (20s, 30s, etc.)
- [ ] Train SST on English Common Voice
	- [ ] Dialect classifier for native **and** non-natives?
	- [ ] Need to double check with the test set: the ASR splits have only a single sentence; the `validated` set includes repetitions. The test set presumably has overlap with validated, it might be worth excluding those _speakers_, not just those sentences